package newModel.bagheri.category;

import newModel.DataBaseCompatible;

public abstract class Category extends DataBaseCompatible {
    public Category(String id, boolean justId) {
//        super(id, justId);
    }
}
